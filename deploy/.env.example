# Copy to deploy/.env. Only used by docker-compose itself (the postgres
# container's superuser credentials) -- the app containers read their own
# .env files from Projet_E-Gestion/.env and projet_website/.env as usual.
POSTGRES_USER=istm
POSTGRES_PASSWORD=change-me
